export type Account = {
    name: string;
    initialAmount: number;
    currentAmount: number;
};

export const View = {
    TRANSACTIONS: "Transactions",
    ACCOUNTS: "Accounts",
    CATEGORIES: "Categories",
    DASHBOARD: "Dashboard",
}
